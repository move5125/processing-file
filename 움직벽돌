int x;
int y;

void setup() {
  size(600,400);
  x = width/2;
}

void draw() {
 background(0,150,25); 
 rectMode(CENTER);
 rect(x, height-10,55,55);
  
}

void keyPressed() {
 if(keyCode == LEFT) { 
 x = x - 10;
 }
 if(keyCode == RIGHT) {
 x = x + 10;
}
println(x);
}
